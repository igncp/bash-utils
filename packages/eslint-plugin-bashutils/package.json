{
  "name": "eslint-plugin-bashutils",
  "private": true,
  "version": "0.0.2",
  "description": "ESLint plugin to parse bash files",
  "author": "Ignacio Carbajo <icarbajop@gmail.com>",
  "license": "MIT",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf lib; ../../node_modules/.bin/babel --config ../../.babelrc.js -d lib src",
    "prepublish": "npm run test && npm run build",
    "test": "sh helpers/test.sh"
  },
  "peerDependencies": {
    "eslint": "^5.6.0"
  },
  "devDependencies": {
    "eslint": "^5.6.0"
  },
  "dependencies": {
    "@bash-utils/parser": "^0.0.2"
  }
}
